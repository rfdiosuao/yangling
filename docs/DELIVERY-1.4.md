# 养令 1.4 · 小芽陪伴

## 入口

- 网站：https://yangling.entermodetwo.com
- APK：https://yangling.entermodetwo.com/downloads/yangling-v1.4-debug.apk
- 包名 `com.yangling.app`，versionName `1.4`，versionCode `5`，沿用此前 Debug 签名。
- APK SHA256：`5f6470e39c197c5d100b243dd5e74170441f10c3e9c75d7a68ebac5a534e3ef4`。

## 已实现

小芽使用内置 GPT 图像工具生成的三张同角色图片：挥手、庆祝、休息。网页和 APK 共用，支持点击问候、拖动、方向键移动、位置保存、收起再展开；18 秒轮换日常休息状态，达标或完成任务时开心轻跳。遵循系统减少动态效果偏好，弹层打开时不遮挡操作。

启动 Logo 下增加：**养令——练对，也练开心。**

收到的 `小芽音频.zip` 中 13 条 MP3 已按批准清单映射：点击、普通鼓励、优秀鼓励、进步、入镜/肩部/腰背/托天纠正、三项完成与全日完成。不会按点击次数虚构完成；每日记录本地保存，跨日归零，同日重复完成不重复奖励。

完整动作稳定 2 秒先普通鼓励；保持稳定至少 8 秒后才进入优秀鼓励，仍受 12 秒全局间隔和同类 30 秒限制。换动作重置比较。姿态分数只是关键点几何参考，不是专业动作认证。

音频与 TTS、冥想音乐共用互斥控制，页面隐藏停止播报。APK 已打入 13 条录音和 3 张图片；网页通过版本化 Service Worker 预缓存，联网后可重试。离线首次打开网页仍需要先有缓存；浏览器清理数据后需要重新下载。

## 验证

- 18 个测试文件、114 项测试通过，包含真实姿态几何到纠正语音的映射、稳定节流、每日去重及跨日、边界约束。
- `scripts/qa-pet.cjs`：393×852 拖动/点击、拖动不误播、收起拖动不误展开、真实完成音频、13 条音频解码与缓存、断网问候、360×740 家长版无横向溢出通过。
- Web、Android 资源构建及 Gradle assembleDebug 成功；APK 内部检查确实包含 13 条 MP3 和 3 张 PNG，签名校验通过。
- 同一浏览器验收脚本已在公网通过；网页备份为 `/var/backups/yangling/20260909-224827`。本次只发布静态网页与 APK，后端配置和密钥未改动。
- 公网 APK 下载 HTTP 200，服务器 SHA256 与本地构建一致。采用 Git 二进制差量在独立暂存目录还原，校验后安装到新版本下载路径；旧版 APK 保留。
- 当前无连接的 Android 真机；尚未验证真机触摸、扬声器播放和后台恢复。浏览器验证不等于真机验收。

## 可补充的一条录音

开弓的手肘可能过高也可能过低，不能复用“再抬高一点”。目前使用本机 TTS：**慢慢调整，让手肘和肩膀保持同高。** 可补交 `pose_elbows_level.mp3`，后续加入离线音频包。现有 13 条录音均已接入，没有缺文件。

原始问答仍使用动态朗读，不缓存用户私人对话。家人绑定、账号同步与多阶段宠物养成不属于本次实现。
