# Task 1: Server configuration and grounded generation

Workdir: F:/AAAI第二曲线沪漂系列/黑客松/复客松/yangling

Implement only server/*.mjs, src/mobile/service.js, src/mobile/config.js, tests/service.test.js, deploy/yangling-api.service and docs/SERVICE.md. Do not edit MobileApp/AdminPanel/CSS/package/deploy nginx; main agent owns integration. No subagents. Commit only your files. Preserve existing user work.

Requirements: Node built-in HTTP server (no new runtime dependencies), bind 127.0.0.1 default port 8789; env YANGLING_DATA_DIR persistent config/audio; YANGLING_ADMIN_TOKEN mandatory for admin mutations with bearer auth, default no token = reject, timing-safe compare. Config JSON atomic writes. Admin key masked/omitted in reads with llm.configured boolean, empty submitted apiKey preserves old key, explicit clearApiKey supported. Credentials never in public config or error logs.

Routes (all start /api): GET /health; GET /config public sanitized normalized config; GET /admin/config authenticated; PUT /admin/config authenticated saves and returns sanitized config; POST /admin/audio raw audio bytes authenticated with Content-Type audio/mpeg, audio/wav, audio/ogg or audio/mp4 and X-Filename, max 20MiB, verify basic file signature, generated safe filename, returns {url,name}; GET /audio/:filename public audio supports Range requests; POST /generate public JSON {kind:'cup'|'move'|'breath'|'question',question:string} returns {lines:string[],sources:[{title,body,reviewed}],generated:boolean,reason:string}. Bound input/body and rate/concurrency upstream calls. CORS only existing production origin, http/https localhost (Capacitor) and local Vite development port5173; no wildcard credentialed requests. Upstream https endpoint only, block loopback/private/link-local addresses and redirects for LLM to avoid service-side request abuse. Timeout and safe fallback. No public arbitrary model/prompt override.

Extend normalizeConfig preserving current schema: audio:[{id,name,url,durationSeconds,enabled}] defaults []; generation:{enabled:false,instructions:'',maxLength:300,fallback:{cup:'...',move:'...',breath:'...'}}; keep courses valid empty video URL as unconfigured, reject example.com links in validateVideoUrl. llm configured boolean survives normalizeConfig but apiKey remains only in server. Public knowledge includes text and sources because user references need display. SourceUrl optional on knowledge normalization. Do not fabricate citations; default builtin example knowledge unreviewed unless explicitly reviewed. Existing configured custom items preserve review flag.

Generation: use findKnowledgeMatches against enabled/reviewed nonempty actual config entries, with kind keywords to find related card sources. No sources or disabled/unconfigured => approved fallback, generated false, no invented sources. Include only retrieved context with numbered citations, request concise lines using existing buildLlmRequest, validate returned citation indices and reject uncited factual lines or nonexistent source indices into fallback. Safety gate existing makePlan/makeAnswer urgent behavior should short-circuit if applicable. Do not claim grounding alone guarantees medical correctness. Upstream errors return safe fallback. No fake provider success tests.

service.js client exports: apiUrl(path) uses import.meta.env.VITE_API_BASE or Capacitor native/GH Pages fallback https://yangling.entermodetwo.com, relative '' same-origin otherwise; fetchPublicConfig(); fetchAdminConfig(token); publishConfig(config,token); uploadAudio(file,token); generateKnowledge(kind,question) all use JSON and errors; normalize public media URL to absolute via apiUrl when url starts /api/audio/. Keep token in memory caller, no localStorage persistence. Signals/timeouts allowed.

Tests first, run expected failures, then implementation and focused tests. Exercise actual local HTTP server/temp directory, auth, restart persistence, key non-disclosure/key preserve/clear, uploads/ranges, validation/fallback, grounded response via injected fetcher for upstream only. Report file docs/superpowers/plans/2026-09-08-service-report.md includes RED/GREEN commands/results, changed files, remaining limitations. Final <=15 lines with status, commit, tests, concerns.
